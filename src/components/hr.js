import React from "react";
import styled from "styled-components";

const HR = styled.div`
  margin-top: 20px;
  padding-top: 20px;
  border-top: 1px solid var(--gallery);
  width: 100%;
  height: 1px;
`;

export default () => <HR />;
